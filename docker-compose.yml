data:
  image: ubuntu
  volumes:
    - /app

web:
  build: .
  volumes:
    - ./app:/code
  volumes_from:
    - data
  ports:
    - "8080:8080"
  command: bash -c "cp Package.swift /app && cp -r Sources /app ; cd /app ; swift build; .build/debug/app"
